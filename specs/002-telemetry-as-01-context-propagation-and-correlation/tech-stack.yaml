# Specification-local tech-stack.yaml
#
# TRANSPORT NOTE: HTTP, gRPC, Kafka, RabbitMQ are declared here because the
# parent capability (001-core-telemetry-domain-model) references them as
# architectural descriptions of AS-02's transport scope. AS-01 itself is
# transport-agnostic (see spec.md Ownership Boundary). These entries are
# allowlisted for validator cross-reference resolution only.

language: Rust
runtime: Tokio
testing: cargo test

frameworks: []
databases: []
transports:
  - HTTP
  - gRPC
  - Kafka
  - RabbitMQ
package_managers: []
sdks:
  - OpenTelemetry
cloud_providers: []
deployment_targets: []
serialization:
  - serde
