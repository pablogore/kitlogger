[package]
name = "kitlogger"
version = "0.1.0"
edition = "2024"

[dependencies]
kit-config = { git = "ssh://git@github.com/pablogore/kit-config.git", default-features = false, features = ["config-core", "config-models"] }
fastrand = "2.4.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
